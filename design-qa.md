# Design QA — The Pharma Coach brand refresh

Current result: local preview passed; the rejected AI-edited lookalike is no longer used on the page. An authentic Jebb candid is cropped into the portrait band. Public development site unchanged.

## Source and capture

- Source visual truth: `/Users/shanee/.codex/generated_images/01a0ca4c-3874-7e33-bf6b-6e85cbaeeaeb/exec-df940300-767b-4978-81aa-0a6aaae0bc32.png` (the selected "Advance the work" concept), 1586 × 992 pixels.
- Owner override: Jebb's 2026-09-21 email "colors for websites and emails + fonts" specifies `#0B0F14`, `#FFFFFF`, `#123B5D`, `#D4AF37`, Manrope headings, and Source Sans 3 body. These take precedence over the mockup's copper color and placeholder typography.
- Browser-rendered implementation: `design-qa-desktop.png`, 1280 × 800 pixels, captured at a 1280 × 800 CSS viewport with Playwright CLI; device scale 1. The in-app Browser was used first, but its resized captures composited duplicate page regions during this copy update, so Playwright supplied the clean QA screenshots.
- Mobile implementation: `design-qa-mobile.png`, 390 × 700 pixels at a short 390 × 700 CSS viewport; device scale 1.
- Portrait-band implementation: `design-qa-portrait.png` is historical and shows the now-rejected AI-edited review candidate. The current authentic-photo crop was inspected in fresh browser captures during the eight-comment update.
- Historical comparison: `design-qa-comparison.png` shows the approved concept against the implementation before the later owner-requested headroom and button-position adjustments. The standalone desktop and mobile screenshots also predate the single-CTA cleanup; the latest state was visually checked in the in-app Browser at 1280 × 800 and 390 × 700.
- State: homepage at top, navigation closed. Primary interactions were also tested in the browser.

## Findings

No remaining P0, P1, or P2 visual or interaction findings. The implementation retains the selected white masthead, photographic full-width hero, oversized left-aligned headline, restrained CTA, and three-column capability band. Shanee's latest approved headline, "Start or advance your career in pharmaceutical sales," intentionally replaces the concept's headline. Supporting copy now includes both new entrants and advancing sales reps.

The five required fidelity surfaces were checked:

- Typography: Manrope headings and Source Sans 3 body match Jebb's email. The large headline and compact navigation keep the intended hierarchy; mobile wrapping is readable and unclipped.
- Spacing and layout: after reducing hero height and widening the content grid, the masthead, hero, and capability strip align with the selected composition. Desktop, tablet (820px), and mobile (390px) were visually checked.
- Colors and tokens: effective site colors use Jebb's black, white, navy, and gold; muted surfaces are transparent navy on white. The mockup's copper was intentionally rejected.
- Image quality: the generated field-sales hero remains; the owner-approved blonde-at-work image has returned to the problem section. The quote band now uses a crop of Jebb's authentic official-site candid, not the rejected synthetic executive image. This resolves likeness but is not a substitute for a genuine executive headshot. Asset provenance is in `public/assets/source/ASSET-SOURCES.md`.
- Copy and content: owner-approved hero wording and matching browser title/metadata are present; program names and displayed prices were retained. The only conversion destination is the existing Med Rep College booking page, labeled “Schedule a call” everywhere. The latest owner comments removed the hero eyebrow and Why Pharma navigation item while retaining the three capability labels.

Focused review of the hero in the comparison image found the head crop and the beginning of the three-column strip to be the main fidelity risks. Both were corrected. No separate focused crop was needed because these details are readable in the 2560-pixel-wide comparison image.

## Comparison history

1. First desktop capture: the 700px hero pushed the three-column strip below the visible fold; the oversized crop cut into the subject's head. The mobile menu also stayed open after an anchor click.
2. Fixes: reduced desktop hero to 610px, widened the desktop content frame, moved image crop to top, and closed the mobile menu on navigation.
3. Post-fix evidence: `design-qa-comparison.png`; mobile screenshot and browser interaction check confirm the menu closes and the `#programs` target appears. FAQ expansion was tested.
4. Owner copy update: the headline, subhead, and page metadata were changed to address both starting and advancing a pharmaceutical-sales career. Clean 1280 × 800 and 390 × 844 screenshots show no clipping or primary-control obstruction. The only Playwright console error was a pre-existing missing `/favicon.ico` (P3, unrelated to this copy change).
5. Owner headroom and fold update: the hero photo was raised 30px, roughly halving the space above the subject at the 777px preview width. The complete copy block was then raised 70px on tablet and 80px on narrow mobile. Clean 1280 × 800 and 390 × 700 captures confirm the gold CTA remains visible without scrolling; it ends around y=557 in the short mobile viewport. Navigation, CTA text, and all other above-the-fold copy remain unchanged.
6. Five browser comments: both primary CTAs now read “Schedule a call” and resolve to the existing Med Rep College discovery-call page; the first capability description includes sales or healthcare experience; the outcome band uses an executive-style Jebb review portrait; the FAQ heading, introduction and answers cover sales and healthcare paths rather than only nursing. Nearby nurse-only copy in the why-pharma and outcome sections was broadened so the page does not contradict the new audience. The booking destination currently advertises a $25 discovery call; no checkout was performed. In-app Browser verified labels, hrefs, image load and accordion expansion. Clean Playwright desktop/mobile renders were used because resized in-app Browser captures duplicated page regions. At 390px, the mobile menu exposes the new booking CTA and has no horizontal overflow (`scrollWidth === innerWidth === 390`). Above-the-fold copy diff: the two CTA labels and the first capability description changed by owner request; header/hero headline, subhead, eyebrow, other navigation items and layout remain the same.
7. One-CTA update: removed distinct access, interview-help, fast-track, and per-program checkout buttons; removed footer email/phone contact actions; retained section navigation and social/reference links. A shared button now repeats one action, “Schedule a call,” at five natural decision points. Browser DOM check confirmed all five have the same booking URL and label, no contact-action links, and no program-card links. Desktop hero, program cards, final CTA, mobile hero, mobile menu, and mobile program entry were visually inspected in the in-app Browser. Clean 1280 × 800 and 390 × 700 Playwright captures in the task work folder were also inspected against the selected concept because the in-app Browser shifted the scroll position during viewport resizing. The program cards are shorter and retain Jebb's approved palette and typography. The hero copy and booking button still fit at 390 × 700. Build and 4/4 Sites tests pass. This is local only; historical screenshots above do not represent the new CTA inventory.
8. Eight browser comments: removed the hero eyebrow and repositioned the headline/copy; removed the entire Why Pharma section and its navigation link; repointed the first capability tile to the existing problem section; removed the vertical capability dividers and the top/bottom outcome-grid rules; restored the owner-approved blonde image; changed the transferability heading; replaced the synthetic Jebb portrait with a crop of the official-site candid; set the programs, FAQ, and final CTA sections to Jebb navy and all five Schedule a call buttons to Jebb gold. Fresh browser captures were reviewed at 777 × 844, 1280 × 800, and 390 × 700. The mobile booking button ends around y=518, no 390px horizontal overflow was found, all internal hash links resolve, the mobile menu closes after Programs navigation, and build plus 4/4 Sites tests pass. Remaining intentional deviation: the authentic Jebb candid is not an executive headshot.
9. Official-site client stories: checked the live [The Pharma Coach success-stories page](https://www.thepharmacoach.com/pharmaceutical-sales-success-stories) in the browser on 2026-09-22 and added attributed excerpts and clearly labeled summaries for Rochelle S. (retail sales to pharma), Emily T. (nursing to pharma), and Alex J. (medical-sales growth). The source is linked under the section; Alex's President's Club result is not presented as a pharmaceutical-sales placement. The section sits between Method and Programs, preserving the owner's single booking CTA and navy final three sections. Browser inspection at the default 777px width and clean Playwright captures at 1280 × 800 and 390 × 700 checked the white/navy/gold palette, Manrope/Source Sans typography, open three-column desktop layout, readable mobile stack, names, claims, and source attribution. No above-the-fold copy changed. The source story page makes client outcome claims; we did not independently verify employment records.
10. Official sliding logos: inspected the live homepage's embedded marquee and copied its 19 distinct logo assets and order into the local site immediately under the sticky header. The original uses a white strip, 48px-tall logos, a continuous linear loop, and pause-on-hover; this implementation keeps those traits while shortening the strip to 76px desktop / 68px mobile to protect the visible hero call button. Two identical visual groups make the loop seamless; reduced-motion users see a static horizontally scrollable set. The region has one accessible description and makes no new affiliation claim. Asset provenance is in `public/assets/source/ASSET-SOURCES.md`. The only new above-the-fold content is the requested logo strip; hero and CTA copy are unchanged. Compared the accepted concept with a clean 1280 × 800 implementation capture: the white header, dark editorial hero, left-aligned headline, woman-on-right crop, and compact gold/navy CTA hierarchy remain; the new white marquee is the deliberate owner-requested addition. A clean 390 × 700 capture found all 38 rendered logo instances loaded, no horizontal page overflow (`scrollWidth === innerWidth === 390`), and the gold call button fully visible (bottom y≈586). Hover paused the animation; reduced-motion emulation removed animation and the duplicate group while allowing horizontal scrolling. The in-app Browser duplicated page regions during viewport resizing, so clean Playwright captures supplied mobile evidence. Build, 4/4 Sites tests, and `git diff --check` pass. Local-only; not published.
11. Owner placement correction: moved the unchanged logo strip below the full woman hero and before the three capability links. Restored `#top` to `<main>` so the wordmark returns to the hero. In-app Browser at 777px now shows header → woman hero → logos, with the hero call button visible. The accepted concept's five relevant features remain intact: white compact header, dark full-width hero, left-aligned headline, woman-right crop, and one gold booking action; only the owner-requested logo strip is newly inserted after the hero. A clean 390 × 700 Playwright render confirmed the mobile sequence hero → logo strip → capability list, all 38 logo instances loaded, no page overflow (`scrollWidth === innerWidth === 390`), and the booking button ending at y≈518. The in-app Browser's resized capture duplicated page regions, so the clean browser render supplied mobile evidence. Hero, navigation, and CTA copy are unchanged. Local-only; not published.

## Follow-up polish

Bio and page-flow correction: removed the early capability strip and consolidated its three headings into “How it works.” The problem headline now reads “Get help landing your next pharma sales role,” with concrete résumé, networking, and interview support. Added a dedicated #about section before How it works, using the portrait from Jebb’s official About page and a source-linked biography and credentials. The navigation About link targets this section. This replaces the prior candid quote band; earlier image notes above are historical. No firm placement deadline was added because official pages report differing outcome timelines. Credentials are attributed to Jebb’s published biography, not independently audited. Fresh 1440px, 777px, and 390px browser checks found no horizontal overflow, broken images, or duplicate early process strip. Desktop/mobile screenshots of the problem, bio, and method sections and a tablet bio screenshot were visually reviewed for crop, wrapping, spacing, and contrast. Build and diff check passed. Local preview only; not published.

Program-card styling correction: replaced the black featured card and transparent navy cards with three white cards carrying navy headings, prices, and body copy. The featured option uses a gold top rule and compact gold label. Title heights and price baselines align at the 777px preview width; mobile resets title height and stacks the cards. In-app Browser visual check at 777px and a clean 390 × 844 Playwright capture passed: all cards white/navy, no horizontal overflow, booking URL and all copy/prices unchanged. Build and diff check passed. Public site unchanged.

Testimonial visibility correction: rechecked the live official success-stories page and confirmed the three local entries for Rochelle S., Emily T., and Alex J. Added a “Testimonials” navigation link and changed the section label from “Client stories” to “Testimonials.” The 777px in-app Browser render shows all three entries and a working anchor; the new navigation item fits without crowding or overflow. Quotes, summaries, source link, and booking CTA are unchanged.

- If Jebb has approved original field or coaching photography, replace the generated editorial images before a production launch. This is not a blocker for the local design preview.
- If Jebb supplies a genuine executive headshot, use it in place of the authentic candid crop; do not revive the synthetic lookalike.
- The 390px program-heading scale could be reduced slightly in a future copy-polish pass, but it remains legible and does not hide controls.

## Implementation checklist

- [x] Exact email palette and font families applied.
- [x] Selected "Advance the work" structure implemented without copying Pfizer or Siemens branding.
- [x] Desktop, tablet, and mobile visually reviewed.
- [x] Navigation, anchor CTA, and FAQ interaction tested.
- [x] Build and Sites worker tests passed.
